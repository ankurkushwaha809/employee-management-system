<%= form_with(model: state) do |form| %>
  <% if state.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(state.errors.count, "error") %> prohibited this state from being saved:</h2>

      <ul>
        <% state.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div>
    <%= form.label :Select_Country, style: "display: block" %>
    <%= form.collection_select :country_id, Country.order(:country_name),:id,:country_name, include_blank: true %>
  </div>
  <div>
    <%= form.label :state_name, style: "display: block" %>
    <%= form.text_field :state_name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
