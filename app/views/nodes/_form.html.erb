<%= form_with(model: node) do |form| %>
  <% if node.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(node.errors.count, "error") %> prohibited this node from being saved:</h2>

      <ul>
        <% node.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :node_name, style: "display: block" %>
    <%= form.text_field :node_name %>
  </div>

  <div>
    <%= form.label :place_id, style: "display: block" %>
    <%= form.number_field :place_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
